import pytest
import warnings
import urllib3
from selenium import webdriver

# Setup credential for other remote webdriver
# user_name = "user_name"
# app_key = "app_key"

# @pytest.fixture(scope=class)

